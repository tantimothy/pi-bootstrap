==========================================================
🏁 NanoClaw Setup Complete
==========================================================

  🌐 Web interface:  http://${HOST_IP}:${NANOCLAW_PORT}
${SERVICE_COMMANDS}

  📲 To add more channels (Telegram, Discord, WhatsApp, Slack, iMessage):
     cd ${INSTALL_PATH} && bash setup/add-telegram.sh
     cd ${INSTALL_PATH} && bash setup/add-discord.sh
     cd ${INSTALL_PATH} && bash setup/add-whatsapp.sh
     cd ${INSTALL_PATH} && bash setup/add-imessage.sh

  🔑 To register a new Anthropic API key:
     cd ${INSTALL_PATH} && bash setup/register-claude-token.sh

  🐳 NanoClaw manages its own Docker containers per conversation group.
     Use the Docker Manager in the deploy menu to view or delete them.
==========================================================
