# MacintoshPi upstream repository to clone. Override for a fork.
MACINTOSHPI_REPO=https://github.com/jaromaz/MacintoshPi

# Optional: pin to a specific branch/tag/commit instead of upstream's
# default branch. Leave empty to always track the latest default branch
# (a shallow clone of its current HEAD).
MACINTOSHPI_REF=
