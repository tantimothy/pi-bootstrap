menu:
  id: ${CONTAINER_NAME:-pi}
  name: "Pi"
  icon: utilities-terminal
deployed_check:
  kind: container
  from_compose_service: pi
entries:
  - id: pi-bootstrap-${CONTAINER_NAME:-pi}
    name: "Pi (SSH)"
    comment: "SSH into the standalone Pi coding-agent container and attach to its persistent tmux session"
    icon: utilities-terminal
    kind: exec
    target: 'bash -c "ssh -p ${SSH_PORT:-2227} pi@localhost"'
    terminal: true
info:
  id: pi-bootstrap-${CONTAINER_NAME:-pi}-info
  name: "Pi Info"
